version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/repository/query.sql"
    schema: "migrations"
    gen:
      go:
        package: "dbgen"
        out: "internal/repository/dbgen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
